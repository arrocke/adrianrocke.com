import { Link } from "gatsby"
import PropTypes from "prop-types"
import React from "react"

const Footer = ({  }) => (
  <footer>
  </footer>
)

Footer.propTypes = {
}

Footer.defaultProps = {
}

export default Footer

